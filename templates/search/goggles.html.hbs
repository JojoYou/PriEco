{{> header }}

<link rel="stylesheet" href="{{ css_path }}settings.css?v={{ css_version }}">
<link rel="stylesheet" href="{{ css_theme }}settings.css?v={{ css_version }}">

{{#if no_js}}
  {{> settings settings}}
{{else}}
  <div id="js_load_settings"></div>
{{/if}}

  <link href="{{ css_path }}goggles.css" rel="stylesheet" type="text/css">
  <div class="background"></div>
  <div class="container">
    <h1 class="title">Goggles</h1>
    <p>Goggles are search filters using which you can prioritize, degrade, or even block domains in PriEco</p>
    <p>They are open source, publicly hosted in a Git repository or freely online</p>
    <p>Here you can find known Goggles you can apply to your searches</p>
    <p>While using PriEco you can create your own Goggles or modify known ones just for you</p>
    <p>Credit to <a href="https://search.brave.com/goggles/discover" target="_blank" class="link">Brave search</a> for creating them</p>
    <p>PriEco adopted their standard so that your Goggles are cross compatible</p>
    <div class="goggle-add">
        <form action="/goggles/load" method="get">
            <input type="url" name="url" placeholder="Paste Goggle URL" required>
            <button type="submit">Add Goggle</button>
        </form>
    </div>
    <form action="/goggles/apply" method="get" id="goggles-form">
        <button type="submit" class="apply-goggles-btn">Apply changes</button>
        <div class="goggles-grid">
        {{#each goggles}}
            <link rel="stylesheet" href="/static/css/goggles_tint.css.hbs?tint={{this.avatar}}&target=g{{@index}}">
            <section class="g{{@index}} center-wrapper">
                <h2>{{this.name}}</h2>
                <i>{{this.author}}</i>
                <br>
                <p>{{this.description}}</p>
                <a href="{{this.source_url}}" target="_blank">Source</a>
                <div class="checkbox_cute">
                    <div class="check">
                        <input id="check_goggle_{{@index}}" name="ids" value="{{this.id}}" type="checkbox"
                               {{#if this.checked}}checked{{/if}}>
                        <label for="check_goggle_{{@index}}"></label>
                    </div>
                </div>
            </section>
        {{/each}}
        </div>
    </form>
  </div>
  {{#unless no_js}}
    <script src="/static/js/goggles.js"></script>
  {{/unless}}
  {{> footer }}
