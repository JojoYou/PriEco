{{#each redirects}}
    <a href="{{this.url}}" target="_blank" rel="noopener noreferrer">
        <span>!{{this.title}}</span>
            <span>{{this.domain}}</span>
        </a>
        <br>
        <br>
{{/each}}
